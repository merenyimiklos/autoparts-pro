import { Role } from '@prisma/client';import { PERMISSIONS } from '../src/auth/permissions';describe('permission matrix',()=>{it('customer cannot edit products',()=>expect(PERMISSIONS[Role.CUSTOMER]).not.toContain('product:write'));it('superadmin can read audit',()=>expect(PERMISSIONS[Role.SUPERADMIN]).toContain('audit:read'));it('warehouse can write inventory',()=>expect(PERMISSIONS[Role.WAREHOUSE]).toContain('inventory:write'))});
