import { Body,Controller,Get,Param,Patch,Query,Req,UseGuards } from '@nestjs/common';import { ReturnStatus,Role } from '@prisma/client';import { Request } from 'express';import { AuthUser } from '../auth/auth.types';import { JwtGuard } from '../auth/jwt.guard';import { RolesGuard } from '../auth/roles.guard';import { Roles } from '../auth/roles.decorator';import { PrismaService } from '../prisma/prisma.service';import { AuditService } from '../audit/audit.service';
@Controller('admin/operations')@UseGuards(JwtGuard,RolesGuard)@Roles(Role.ADMIN,Role.SUPERADMIN)export class OperationsController{constructor(private prisma:PrismaService,private audit:AuditService){}@Get('users')users(@Query('q')q?:string){return this.prisma.user.findMany({where:q?{OR:[{email:{contains:q,mode:'insensitive'}},{firstName:{contains:q,mode:'insensitive'}},{lastName:{contains:q,mode:'insensitive'}}]}:{},take:100,select:{id:true,email:true,firstName:true,lastName:true,role:true,emailVerifiedAt:true,createdAt:true}})}@Patch('users/:id/role')@Roles(Role.SUPERADMIN)async role(@Req()r:Request&{user:AuthUser},@Param('id')id:string,@Body()b:{role:Role}){const before=await this.prisma.user.findUniqueOrThrow({where:{id}});const u=await this.prisma.user.update({where:{id},data:{role:b.role}});await this.audit.write({actorId:r.user.id,action:'USER_ROLE_CHANGED',entityType:'User',entityId:id,before:{role:before.role},after:{role:u.role}});return{ok:true}}@Get('reviews')reviews(){return this.prisma.review.findMany({take:100,include:{user:{select:{email:true}},product:{select:{name:true,sku:true}}},orderBy:{createdAt:'desc'}})}@Patch('reviews/:id')review(@Param('id')id:string,@Body()b:{approved:boolean}){return this.prisma.review.update({where:{id},data:{approved:b.approved}})}@Get('returns')returns(){return this.prisma.returnRequest.findMany({take:100,include:{order:true,user:{select:{email:true}}},orderBy:{createdAt:'desc'}})}@Patch('returns/:id')returnStatus(@Param('id')id:string,@Body()b:{status:ReturnStatus;note?:string}){return this.prisma.returnRequest.update({where:{id},data:{status:b.status,note:b.note}})}@Get('audit')@Roles(Role.SUPERADMIN)auditLog(){return this.prisma.auditLog.findMany({take:200,include:{actor:{select:{email:true}}},orderBy:{createdAt:'desc'}})}}
