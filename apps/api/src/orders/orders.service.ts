import { BadRequestException,ForbiddenException,Injectable,NotFoundException } from '@nestjs/common';import { OrderStatus,PaymentStatus,Prisma,Role } from '@prisma/client';import { nanoid } from 'nanoid';import { PrismaService } from '../prisma/prisma.service';import { InventoryService } from '../inventory/inventory.service';import { PricingService } from '../pricing/pricing.service';import { MailService } from '../mail/mail.service';import { AuditService } from '../audit/audit.service';import { canTransition } from './order-state';
@Injectable()export class OrdersService{constructor(private prisma:PrismaService,private inventory:InventoryService,private pricing:PricingService,private mail:MailService,private audit:AuditService){}
async checkout(userId:string,dto:{email:string;shippingAddress:any;billingAddress:any;paymentMethod:string;shippingMethod:string}){const result=await this.prisma.$transaction(async tx=>{const cart=await tx.cart.findUnique({where:{id:`user-${userId}`},include:{items:{include:{product:true}}}});if(!cart?.items.length)throw new BadRequestException('A kosár üres.');const quote=await this.pricing.quote(cart.items.map(i=>({productId:i.productId,quantity:i.quantity,grossPrice:Number(i.product.grossPrice)})),cart.couponCode??undefined,userId,dto.shippingMethod,tx);const status=dto.paymentMethod==='cod'?OrderStatus.PROCESSING:OrderStatus.AWAITING_PAYMENT;const order=await tx.order.create({data:{orderNumber:`AP-${new Date().toISOString().slice(0,10).replaceAll('-','')}-${nanoid(6).toUpperCase()}`,userId,email:dto.email,status,paymentStatus:PaymentStatus.PENDING,subtotalGross:quote.subtotalGross,discountGross:quote.discountGross,shippingGross:quote.shippingGross,totalGross:quote.totalGross,paymentMethod:dto.paymentMethod,shippingMethod:dto.shippingMethod,shippingAddress:dto.shippingAddress,billingAddress:dto.billingAddress,items:{create:cart.items.map(i=>({productId:i.productId,sku:i.product.sku,productName:i.product.name,quantity:i.quantity,unitNet:i.product.netPrice,vatRate:i.product.vatRate,unitGross:i.product.grossPrice,discountGross:0,lineGross:Number(i.product.grossPrice)*i.quantity}))},history:{create:{toStatus:status,actorId:userId,customerNote:'Rendelés létrehozva'}}}});for(const i of cart.items)await this.inventory.reserve(tx,i.productId,i.quantity,order.id,userId);if(quote.couponId)await this.pricing.consumeCoupon(quote.couponId,userId,order.id,tx);await tx.cartItem.deleteMany({where:{cartId:cart.id}});await tx.cart.update({where:{id:cart.id},data:{couponCode:null}});await this.audit.write({actorId:userId,action:'ORDER_CREATED',entityType:'Order',entityId:order.id,after:{orderNumber:order.orderNumber,total:quote.totalGross}},tx);return order},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable,maxWait:5000,timeout:10000});void this.mail.sendOrder(dto.email,result.orderNumber,Number(result.totalGross)).catch(()=>undefined);return result}
async mine(userId:string){return this.prisma.order.findMany({where:{userId},include:{items:true,history:{orderBy:{createdAt:'asc'}}},orderBy:{createdAt:'desc'}})}async adminList(){return this.prisma.order.findMany({take:100,include:{items:true,history:{orderBy:{createdAt:'asc'}}},orderBy:{createdAt:'desc'}})}
async transition(orderId:string,to:OrderStatus,actor:{id:string;role:Role},notes?:{customer?:string;internal?:string}){const updated=await this.prisma.$transaction(async tx=>{const order=await tx.order.findUnique({where:{id:orderId}});if(!order)throw new NotFoundException();if(!canTransition(order.status,to))throw new BadRequestException(`Érvénytelen állapotváltás: ${order.status} -> ${to}`);if(actor.role===Role.WAREHOUSE&&!([OrderStatus.PICKING,OrderStatus.PACKED,OrderStatus.HANDED_TO_COURIER,OrderStatus.READY_FOR_PICKUP] as OrderStatus[]).includes(to))throw new ForbiddenException();if(to===OrderStatus.CANCELLED)await this.inventory.releaseOrder(tx,orderId,actor.id);if(to===OrderStatus.PACKED)await this.inventory.commitOrder(tx,orderId,actor.id);const changed=await tx.order.update({where:{id:orderId},data:{status:to}});await tx.orderStatusHistory.create({data:{orderId,fromStatus:order.status,toStatus:to,actorId:actor.id,customerNote:notes?.customer,internalNote:notes?.internal}});await this.audit.write({actorId:actor.id,action:'ORDER_STATUS_CHANGED',entityType:'Order',entityId:orderId,before:{status:order.status},after:{status:to}},tx);return changed});void this.mail.sendStatus(updated.email,updated.orderNumber,to).catch(()=>undefined);return updated}}
