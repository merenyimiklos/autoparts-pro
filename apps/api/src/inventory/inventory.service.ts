import { BadRequestException,Injectable } from '@nestjs/common';import { InventoryMovementType,Prisma } from '@prisma/client';import { PrismaService } from '../prisma/prisma.service';import { AuditService } from '../audit/audit.service';
@Injectable()export class InventoryService{constructor(private prisma:PrismaService,private audit:AuditService){}
async reserve(tx:Prisma.TransactionClient,productId:string,qty:number,orderId:string,actorId?:string){const warehouse=await tx.warehouse.findFirst({where:{active:true},orderBy:{code:'asc'}});if(!warehouse)throw new BadRequestException('Nincs aktív raktár.');const before=await tx.inventoryBalance.findUnique({where:{productId_warehouseId:{productId,warehouseId:warehouse.id}}});if(!before)throw new BadRequestException('Nincs készlet.');const changed=await tx.$executeRaw`UPDATE "InventoryBalance" SET "reserved"="reserved"+${qty} WHERE "productId"=${productId} AND "warehouseId"=${warehouse.id} AND ("physical"-"reserved"-"damaged")>=${qty}`;if(changed!==1)throw new BadRequestException('Nincs elegendő elérhető készlet.');const after=await tx.inventoryBalance.findUniqueOrThrow({where:{productId_warehouseId:{productId,warehouseId:warehouse.id}}});await tx.inventoryMovement.create({data:{productId,warehouseId:warehouse.id,type:InventoryMovementType.RESERVATION,quantity:qty,previousPhysical:before.physical,newPhysical:after.physical,previousReserved:before.reserved,newReserved:after.reserved,orderId,actorId}})}
async releaseOrder(tx:Prisma.TransactionClient,orderId:string,actorId?:string){const reservations=await tx.inventoryMovement.findMany({where:{orderId,type:InventoryMovementType.RESERVATION}});for(const m of reservations){const terminal=await tx.inventoryMovement.findFirst({where:{orderId,productId:m.productId,warehouseId:m.warehouseId,type:{in:[InventoryMovementType.RELEASE,InventoryMovementType.SALE]}}});if(terminal)continue;const b=await tx.inventoryBalance.findUniqueOrThrow({where:{productId_warehouseId:{productId:m.productId,warehouseId:m.warehouseId}}});const qty=Math.min(m.quantity,b.reserved);if(qty<=0)continue;await tx.inventoryBalance.update({where:{productId_warehouseId:{productId:m.productId,warehouseId:m.warehouseId}},data:{reserved:{decrement:qty}}});await tx.inventoryMovement.create({data:{productId:m.productId,warehouseId:m.warehouseId,type:InventoryMovementType.RELEASE,quantity:qty,previousPhysical:b.physical,newPhysical:b.physical,previousReserved:b.reserved,newReserved:b.reserved-qty,orderId,actorId,reason:'ORDER_CANCELLED'}})}}
async commitOrder(tx:Prisma.TransactionClient,orderId:string,actorId?:string){const reservations=await tx.inventoryMovement.findMany({where:{orderId,type:InventoryMovementType.RESERVATION}});for(const m of reservations){const sale=await tx.inventoryMovement.findFirst({where:{orderId,productId:m.productId,warehouseId:m.warehouseId,type:InventoryMovementType.SALE}});if(sale)continue;const b=await tx.inventoryBalance.findUniqueOrThrow({where:{productId_warehouseId:{productId:m.productId,warehouseId:m.warehouseId}}});if(b.reserved<m.quantity||b.physical<m.quantity)throw new BadRequestException('A foglalt készlet nem teljesíthető.');await tx.inventoryBalance.update({where:{productId_warehouseId:{productId:m.productId,warehouseId:m.warehouseId}},data:{physical:{decrement:m.quantity},reserved:{decrement:m.quantity}}});await tx.inventoryMovement.create({data:{productId:m.productId,warehouseId:m.warehouseId,type:InventoryMovementType.SALE,quantity:m.quantity,previousPhysical:b.physical,newPhysical:b.physical-m.quantity,previousReserved:b.reserved,newReserved:b.reserved-m.quantity,orderId,actorId}})}}
async adjust(actorId:string,dto:{productId:string;warehouseId:string;type:InventoryMovementType;quantity:number;reason:string;note?:string}){if(!Number.isInteger(dto.quantity)||dto.quantity===0)throw new BadRequestException('A mennyiség nem lehet 0.');return this.prisma.$transaction(async tx=>{const b=await tx.inventoryBalance.findUniqueOrThrow({where:{productId_warehouseId:{productId:dto.productId,warehouseId:dto.warehouseId}}});const delta=[InventoryMovementType.RECEIPT,InventoryMovementType.RETURN,InventoryMovementType.TRANSFER_IN].includes(dto.type)?Math.abs(dto.quantity):[InventoryMovementType.SCRAP,InventoryMovementType.TRANSFER_OUT].includes(dto.type)?-Math.abs(dto.quantity):dto.quantity;if(b.physical+delta<0)throw new BadRequestException('A fizikai készlet nem mehet negatívba.');if(b.physical+delta-b.reserved-b.damaged<0)throw new BadRequestException('A módosítás a foglalt/sérült készlet miatt túlfoglalást okozna.');const after=await tx.inventoryBalance.update({where:{productId_warehouseId:{productId:dto.productId,warehouseId:dto.warehouseId}},data:{physical:{increment:delta}}});const move=await tx.inventoryMovement.create({data:{productId:dto.productId,warehouseId:dto.warehouseId,type:dto.type,quantity:Math.abs(dto.quantity),previousPhysical:b.physical,newPhysical:after.physical,previousReserved:b.reserved,newReserved:b.reserved,actorId,reason:dto.reason,note:dto.note}});await this.audit.write({actorId,action:'INVENTORY_ADJUSTED',entityType:'InventoryBalance',entityId:b.id,before:{physical:b.physical},after:{physical:after.physical,type:dto.type,quantity:dto.quantity}},tx);return move})}}
