'use client';import {useEffect,useState} from 'react';import {api} from '../../lib/api';export default function Profile(){const[p,setP]=useState<any>(null);useEffect(()=>{api('/customer/profile').then(setP).catch(()=>{})},[]);if(!p)return <main className="section"><div className="container">Jelentkezz be a profil megtekintéséhez.</div></main>;return <main className="section"><div className="container two"><div className="panel"><h1>{p.lastName} {p.firstName}</h1><p>{p.email}</p><p className="muted">Szerepkör: {p.role}</p></div><div className="panel"><h2>Címeim</h2>{p.addresses?.length?p.addresses.map((a:any)=><p key={a.id}>{a.label}: {a.postalCode} {a.city}, {a.street}</p>):<p className="muted">Még nincs mentett cím.</p>}</div></div></main>}
