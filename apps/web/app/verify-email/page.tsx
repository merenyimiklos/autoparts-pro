'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {api} from '../../lib/api';export default function Verify(){const[msg,setMsg]=useState('Ellenőrzés…');useEffect(()=>{const token=new URLSearchParams(location.search).get('token');if(!token){setMsg('Hiányzó token.');return}api('/auth/verify-email',{method:'POST',body:JSON.stringify({token})}).then(()=>setMsg('Az e-mail-cím megerősítve.')).catch(()=>setMsg('A link hibás vagy lejárt.'))},[]);return <main className="section"><div className="container panel"><h1>E-mail megerősítése</h1><p>{msg}</p><Link className="button" href="/login">Belépés</Link></div></main>}
