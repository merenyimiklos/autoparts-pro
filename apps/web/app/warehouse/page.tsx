'use client';
import { FormEvent,useEffect,useState } from 'react';import {api} from '../../lib/api';
export default function Warehouse(){const[b,setB]=useState<any[]>([]),[m,setM]=useState<any[]>([]),[msg,setMsg]=useState('');const load=()=>Promise.all([api<any[]>('/inventory/balances').then(setB),api<any[]>('/inventory/movements').then(setM)]).catch(()=>setMsg('Raktáros vagy admin jogosultság szükséges.'));useEffect(()=>{void load()},[]);async function adjust(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),row=b.find(x=>x.id===f.get('balanceId'));if(!row)return;await api('/inventory/adjust',{method:'POST',body:JSON.stringify({productId:row.productId,warehouseId:row.warehouseId,type:f.get('type'),quantity:Number(f.get('quantity')),reason:f.get('reason')})});setMsg('Készletmozgás rögzítve.');void load()}return <main className="section"><div className="container"><h1>Raktár</h1><form className="panel stack" onSubmit={adjust}><h2>Bevételezés / korrekció / selejtezés</h2><select className="select" name="balanceId" required><option value="">Termék és raktár…</option>{b.map(x=><option key={x.id} value={x.id}>{x.product.sku} – {x.product.name} · {x.warehouse.code}</option>)}</select><div className="two"><select className="select" name="type"><option value="RECEIPT">Bevételezés</option><option value="ADJUSTMENT">Leltárkorrekció</option><option value="SCRAP">Selejtezés</option><option value="RETURN">Visszáru</option></select><input className="input" name="quantity" type="number" required placeholder="Mennyiség"/></div><input className="input" name="reason" required placeholder="Indok"/><button className="button">Készletmozgás rögzítése</button>{msg&&<p>{msg}</p>}</form><h2>Készletek</h2><div style={{overflowX:'auto'}}><table><thead><tr><th>Termék</th><th>Raktár</th><th>Fizikai</th><th>Foglalt</th><th>Sérült</th><th>Várható</th><th>Elérhető</th></tr></thead><tbody>{b.map(x=><tr key={x.id}><td>{x.product.name}</td><td>{x.warehouse.code}</td><td>{x.physical}</td><td>{x.reserved}</td><td>{x.damaged}</td><td>{x.incoming}</td><td>{x.physical-x.reserved-x.damaged}</td></tr>)}</tbody></table></div><h2 style={{marginTop:28}}>Legutóbbi mozgások</h2><div className="panel">{m.slice(0,20).map(x=><div className="cartrow" key={x.id}><span>{x.type} · {x.productId} · {x.reason??''}</span><strong>{x.quantity} db</strong></div>)}</div></div></main>}
