'use client';import {FormEvent,useState} from 'react';import {api} from '../../lib/api';export default function Checkout(){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const address={recipient:f.get('recipient'),postalCode:f.get('postalCode'),city:f.get('city'),street:f.get('street'),country:'HU'};try{const o=await api<{orderNumber:string}>('/orders/checkout',{method:'POST',body:JSON.stringify({email:f.get('email'),shippingAddress:address,billingAddress:address,paymentMethod:f.get('paymentMethod'),shippingMethod:f.get('shippingMethod')})});setMsg(`Sikeres rendelés: ${o.orderNumber}`)}catch(err){setMsg(err instanceof Error?err.message:'Hiba')}}return <main className="section"><div className="container"><form className="panel stack" onSubmit={submit}><h1>Pénztár</h1><input className="input" name="email" type="email" placeholder="E-mail" required/><input className="input" name="recipient" placeholder="Átvevő neve" required/><div className="two"><input className="input" name="postalCode" placeholder="Irányítószám" required/><input className="input" name="city" placeholder="Város" required/></div><input className="input" name="street" placeholder="Utca, házszám" required/><select className="select" name="shippingMethod"><option value="home">Házhoz szállítás – 1 990 Ft</option><option value="pickup">Személyes átvétel – ingyenes</option><option value="parcel">Csomagpont – 1 490 Ft</option></select><select className="select" name="paymentMethod"><option value="cod">Utánvét</option><option value="transfer">Banki átutalás</option><option value="mock-card">Mock bankkártya</option></select><button className="button">Rendelés leadása</button>{msg&&<p>{msg}</p>}</form></div></main>}
