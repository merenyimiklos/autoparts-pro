'use client';import {useEffect,useState} from 'react';import {api} from '../../lib/api';export default function Orders(){const[x,setX]=useState<any[]>([]);useEffect(()=>{api<any[]>('/orders/mine').then(setX).catch(()=>{})},[]);return <main className="section"><div className="container"><h1>Rendeléseim</h1>{x.map(o=><div className="panel" style={{marginBottom:12}} key={o.id}><strong>{o.orderNumber}</strong><p>{o.status} · {Number(o.totalGross).toLocaleString('hu-HU')} Ft</p></div>)}</div></main>}
