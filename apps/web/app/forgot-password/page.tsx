'use client';import {FormEvent,useState} from 'react';import {api} from '../../lib/api';export default function Forgot(){const[msg,setMsg]=useState('');return <main className="section"><div className="container"><form className="panel stack" style={{maxWidth:480,margin:'0 auto'}} onSubmit={async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);await api('/auth/forgot-password',{method:'POST',body:JSON.stringify({email:f.get('email')})});setMsg('Ha létezik a fiók, elküldtük a visszaállító levelet.')}}><h1>Elfelejtett jelszó</h1><input className="input" name="email" type="email" placeholder="E-mail" required/><button className="button">Visszaállító levél</button>{msg&&<p>{msg}</p>}</form></div></main>}
