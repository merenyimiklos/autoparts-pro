'use client';
import { FormEvent,useEffect,useState } from 'react';import {api} from '../../../lib/api';
export default function Settings(){const[w,setW]=useState<any[]>([]),[s,setS]=useState<any[]>([]),[msg,setMsg]=useState('');const load=()=>Promise.all([api<any[]>('/admin/operations/warehouses').then(setW),api<any[]>('/admin/operations/settings').then(setS)]).catch(()=>setMsg('Ehhez szuperadmin jogosultság szükséges.'));useEffect(()=>{void load()},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api('/admin/operations/warehouses',{method:'POST',body:JSON.stringify({code:f.get('code'),name:f.get('name'),address:f.get('address')})});setMsg('Telephely létrehozva.');void load()}return <main className="section"><div className="container"><h1>Rendszer és telephelyek</h1>{msg&&<p>{msg}</p>}<form className="panel stack" onSubmit={add}><h2>Új raktár/telephely</h2><input className="input" name="code" placeholder="Kód" required/><input className="input" name="name" placeholder="Név" required/><input className="input" name="address" placeholder="Cím" required/><button className="button">Létrehozás</button></form><h2>Telephelyek</h2>{w.map(x=><div className="panel" key={x.id} style={{marginBottom:10}}><strong>{x.code} – {x.name}</strong><div className="muted">{x.address} · {x.active?'aktív':'inaktív'}</div></div>)}<h2>Rendszerbeállítások</h2>{s.map(x=><pre className="panel" key={x.key}>{x.key}: {JSON.stringify(x.value,null,2)}</pre>)}</div></main>}
