'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {api} from '../../lib/api';export default function Admin(){const[d,setD]=useState<any>(null);useEffect(()=>{api('/admin/dashboard').then(setD).catch(()=>{})},[]);return <main className="section"><div className="container"><h1>Admin dashboard</h1><div className="grid">{[['Mai bevétel',d?.revenue?.today],['Heti bevétel',d?.revenue?.week],['Havi bevétel',d?.revenue?.month],['Függő rendelés',d?.orders?.pending]].map(([n,v])=><div className="panel" key={String(n)}><div className="muted">{n}</div><div className="price">{typeof v==='number'&&String(n).includes('bevétel')?`${v.toLocaleString('hu-HU')} Ft`:v??'—'}</div></div>)}</div><div className="toolbar" style={{marginTop:24}}><Link className="button" href="/admin/orders">Rendelések</Link><Link className="button secondary" href="/admin/products">Termékek</Link><Link className="button secondary" href="/warehouse">Raktár</Link></div></div></main>}
