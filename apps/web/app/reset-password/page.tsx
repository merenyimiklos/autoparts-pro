'use client';import {FormEvent,useState} from 'react';import {api} from '../../lib/api';export default function Reset(){const[msg,setMsg]=useState('');return <main className="section"><div className="container"><form className="panel stack" style={{maxWidth:480,margin:'0 auto'}} onSubmit={async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const token=new URLSearchParams(location.search).get('token');await api('/auth/reset-password',{method:'POST',body:JSON.stringify({token,password:f.get('password')})});setMsg('A jelszó megváltozott.')}}><h1>Új jelszó</h1><input className="input" name="password" type="password" minLength={8} required/><button className="button">Jelszó mentése</button>{msg&&<p>{msg}</p>}</form></div></main>}
