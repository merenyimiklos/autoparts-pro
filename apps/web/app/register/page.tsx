'use client';import {FormEvent,useState} from 'react';import Link from 'next/link';import {api} from '../../lib/api';export default function Register(){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api('/auth/register',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password'),firstName:f.get('firstName'),lastName:f.get('lastName')})});setMsg('Sikeres regisztráció. A Mailpitben találod a megerősítő levelet.')}catch(err){setMsg(err instanceof Error?err.message:'Hiba')}}return <main className="section"><div className="container"><form className="panel stack" style={{maxWidth:520,margin:'0 auto'}} onSubmit={submit}><h1>Regisztráció</h1><label>Vezetéknév<input className="input" name="lastName" required/></label><label>Keresztnév<input className="input" name="firstName" required/></label><label>E-mail<input className="input" name="email" type="email" required/></label><label>Jelszó<input className="input" name="password" type="password" minLength={8} required/></label><button className="button">Fiók létrehozása</button>{msg&&<p>{msg}</p>}<Link className="muted" href="/login">Már van fiókod? Belépés</Link></form></div></main>}
