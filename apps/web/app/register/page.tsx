'use client';import {FormEvent,useState} from 'react';import Link from 'next/link';import {api} from '../../lib/api';import {notify} from '../../components/Feedback';export default function Register(){const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{await api('/auth/register',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password'),firstName:f.get('firstName'),lastName:f.get('lastName')})});notify('success','Sikeres regisztráció','A Mailpitben találod a megerősítő levelet.');e.currentTarget.reset()}catch(err){notify('error','Nem sikerült regisztrálni',err instanceof Error?err.message:'Ellenőrizd az adatokat.')}finally{setBusy(false)}}return <main className="section"><div className="container"><form className="panel stack" style={{maxWidth:520,margin:'0 auto'}} onSubmit={submit}><span className="eyebrow">Új fiók</span><h1>Regisztráció</h1><label className="field-label">Vezetéknév<input className="input" name="lastName" required/></label><label className="field-label">Keresztnév<input className="input" name="firstName" required/></label><label className="field-label">E-mail<input className="input" name="email" type="email" required/></label><label className="field-label">Jelszó<input className="input" name="password" type="password" minLength={8} required/></label><button disabled={busy} className="button">{busy?'Fiók létrehozása…':'Fiók létrehozása'}</button><Link className="muted" href="/login">Már van fiókod? Belépés</Link></form></div></main>}
