'use client';import {FormEvent,useState} from 'react';import {api} from '../../lib/api';export default function Login(){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api('/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});location.href='/products'}catch{setMsg('Sikertelen belépés.')}}return <main className="section"><div className="container"><form onSubmit={submit} className="panel stack" style={{maxWidth:440,margin:'0 auto'}}><h1>Belépés</h1><input className="input" name="email" type="email" defaultValue="customer@autoparts.local" required/><input className="input" name="password" type="password" defaultValue="Demo1234!" required/><button className="button">Belépés</button>{msg&&<p className="muted">{msg}</p>}</form></div></main>}
