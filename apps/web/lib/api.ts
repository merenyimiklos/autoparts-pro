export const API=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000/api';export const SERVER_API=process.env.INTERNAL_API_URL??API;
async function readError(r:Response){const text=await r.text();if(!text)return `HTTP ${r.status}`;try{const parsed=JSON.parse(text) as {message?:string|string[];error?:string};if(Array.isArray(parsed.message))return parsed.message.join(' ');if(parsed.message)return parsed.message;if(parsed.error)return parsed.error}catch{}return text}
export async function api<T>(path:string,init?:RequestInit):Promise<T>{const r=await fetch(`${API}${path}`,{...init,credentials:'include',headers:{'Content-Type':'application/json',...(init?.headers??{})}});if(!r.ok)throw new Error(await readError(r));if(r.status===204)return undefined as T;return r.json() as Promise<T>}
export type Product={id:string;name:string;slug:string;sku:string;shortDescription:string;grossPrice:string|number;salePrice?:number;promotion?:string;available:number;fits?:boolean;manufacturer:{name:string};images:{url:string;alt?:string}[];specs?:Record<string,unknown>};
export type CartItem={id:string;productId:string;quantity:number;product:{id:string;name:string;slug?:string;grossPrice:number|string;images?:{url:string;alt?:string}[]}};
export type CartResponse={items:CartItem[];itemCount:number;isEmpty:boolean;couponCode?:string|null;subtotalGross:number;discountGross:number;shippingGross:number;totalGross:number};
