export const API=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000/api';export const SERVER_API=process.env.INTERNAL_API_URL??API;export async function api<T>(path:string,init?:RequestInit):Promise<T>{const r=await fetch(`${API}${path}`,{...init,credentials:'include',headers:{'Content-Type':'application/json',...(init?.headers??{})}});if(!r.ok)throw new Error((await r.text())||`HTTP ${r.status}`);return r.json() as Promise<T>}
export type Product={id:string;name:string;slug:string;sku:string;shortDescription:string;grossPrice:string|number;salePrice?:number;promotion?:string;available:number;fits?:boolean;manufacturer:{name:string};images:{url:string;alt?:string}[];specs?:Record<string,unknown>};
