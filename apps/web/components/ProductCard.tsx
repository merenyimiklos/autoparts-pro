import Link from 'next/link';import type { Product } from '../lib/api';import { CatalogActions } from './CatalogActions';export function ProductCard({p}:{p:Product}){return <article className="card"><img src={p.images[0]?.url??'/product-placeholder.svg'} alt={p.images[0]?.alt??p.name}/><div className="pad stack"><div className="muted">{p.manufacturer.name} · {p.sku}</div><Link href={`/products/${p.slug}`}><strong>{p.name}</strong></Link>{p.fits===true&&<span className="pill">✓ Biztosan megfelelő</span>}<div className="price">{Number(p.grossPrice).toLocaleString('hu-HU')} Ft</div><div className="muted">{p.available>0?`${p.available} db elérhető`:'Jelenleg nincs készleten'}</div><CatalogActions product={p}/></div></article>}
