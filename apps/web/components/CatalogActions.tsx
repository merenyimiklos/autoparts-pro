'use client';import {useEffect,useState} from 'react';type Mini={id:string;name:string;slug:string;sku:string;grossPrice:number|string;manufacturer:{name:string};specs?:unknown};function add(key:string,p:Mini,max=20){const old=JSON.parse(localStorage.getItem(key)||'[]') as Mini[];localStorage.setItem(key,JSON.stringify([p,...old.filter(x=>x.id!==p.id)].slice(0,max)))}export function CatalogActions({product}:{product:Mini}){const[msg,setMsg]=useState('');return <div style={{display:'flex',gap:8,flexWrap:'wrap'}}><button className="button secondary" type="button" onClick={()=>{add('autoparts:favorites',product);setMsg('Kedvencekhez adva')}}>♡ Kedvenc</button><button className="button secondary" type="button" onClick={()=>{add('autoparts:compare',product,4);setMsg('Összehasonlításhoz adva')}}>⇄ Összevetés</button>{msg&&<small className="muted">{msg}</small>}</div>}
export function RememberViewed({product}:{product:Mini}){useEffect(()=>{add('autoparts:recent',product,12)},[product]);return null}
