'use client';import { useState } from 'react';import { api } from '../lib/api';export function AddToCart({productId,disabled}:{productId:string;disabled?:boolean}){const [msg,setMsg]=useState('');return <div className="stack"><button disabled={disabled} className="button" onClick={async()=>{try{await api('/cart/items',{method:'POST',body:JSON.stringify({productId,quantity:1})});setMsg('Kosárba téve.')}catch{setMsg('Nem sikerült a kosárba tenni.')}}}>Kosárba</button>{msg&&<span className="muted">{msg}</span>}</div>}
